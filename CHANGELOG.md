# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/) 格式（自 0.1.0 起）。

## [0.5.0] - 2026-09-14

豆社社区版：受治理的公开作品发布与互动（D49）、评论内容安全（D50，腾讯云 TMS）、同意优先匿名分析、账号治理与官方批次，配合全站视觉重塑与性能加固。

### 新增

- 豆社公开作品全链路：投稿（必须携带原图，D49）、不可变修订与人工审核、引用（私人副本）、点赞、评论分页、举报与处理、标签分类、作品下架/恢复与评论锁
- 评论内容安全（D50）：腾讯云文本内容安全 TextModeration 判定（Pass/Review/Block）；成本护栏（同文 7 天缓存、全站每日预算、每账号/IP 反刷闸门）；每次判定写审计（来源、建议、标签、置信度、命中词、RequestId）；未配置或调用失败时评论一律转人工待审，绝不静默公开
- 同意优先匿名分析：明确同意后才建立匿名访客身份（只存随机令牌哈希），30 分钟会话窗口与有序漏斗；管理后台分析仪表板（趋势/维度/转化）
- 账号治理：管理员角色授予与撤销（CLI + 后台，禁止移除最后一名有效管理员）、账号暂停/恢复（撤销全部会话）、账号匿名化（去身份保留公开作品与治理记录）；人员操作需输入目标 userId 二次确认
- 官方批次：管理员在浏览器本地批量生成官方草稿，服务器不接收批次原图、文件名或裁剪源；生成完成/取消与草稿发布独立，失败项可按本地源重试；发布前上传原图
- 作品原图独立存储：投稿原图进私有 COS 桶（或本地目录），服务端按魔数嗅探类型，按作者/审核/引用关系控制取回
- 管理后台重构：总览页、审核台、治理台（评论/举报/标签/人员/审计/系统），共享组件与键盘可达性
- UI 品牌重塑：设计 token（五级表面、阴影尺、动效尺）、Button/IconButton 按钮体系与豆粒图标、品牌化控件（日期/数字/折叠/复选/分段滑块/芯片/徽标）、发丝边与错峰进场；首页选图落区改为钉板
- 字体离线交付：Noto Sans CJK SC 与寒蝉全圆体随仓库交付，完整性校验门禁
- 安全：豆社公开内容节流与写接口限流（ADR-0021）

### 变更

- 评论审核外部化：旧「审核规则版本」字面词表机制与 `/admin/rules` 页面移除，`moderation_rule_set_versions` 表只读保留（D50）
- 移除评论编辑能力，评论只能发表与删除（ADR-0022）
- 社区规范与隐私政策更新：评论文本送第三方判定并保留判定记录；公开作品原图存储与删除约定
- TMS 凭证默认沿用 COS 子账号密钥（CAM 追加 `tms:TextModeration` 即可），`TMS_SECRET_ID/KEY/REGION` 仅用于单独指定；docker compose 空字符串语义按未设置处理
- 官方批次改为分步流程，缩略图改服务端带格线 PNG

### 修复与性能

- 工作台取消按钮状态下沉到子组件，点击处理器 188ms → 0.7ms；裁剪首帧分条带合成；图纸落地提交过渡渲染
- 分析时间范围报表加索引；裁剪预览避免解码尺寸重采样
- 修复大量 E2E/CI 门禁：长任务归因与阈值、生产冒烟 CSP/覆盖率红灯、E2E 稳定三连跑预算 150 分钟


## [0.4.0] - 2026-08-30

色板目录与移动画布扩展版：13 套内置色板、2.6 mm Mini 板型、有界移动画布、不透明 PNG 与分包 ZIP 导出，以及严格 v3 项目协议。

### 新增

- 内置色板扩至 13 套：五套豆谱经典版（MARD、COCO、漫漫、盼盼、咪小窝）+ MARD 291 / COCO 291 / 漫漫 278 / 盼盼 289 / 咪小窝 290 / MARD 221 核对版 / Artkal C 197 / Artkal M 221；外部数据锁定于 `HansBug/pindou-color-data@178dafb`（附来源与许可记录），色板页展示收录数、可生成数与排除项；自定义色板导入
- 制作规格（板型档位）稳定 ID `5mm-29` / `2.6mm-50` / `2.6mm-52`，统一贯穿板缝、跟拼分板、尺寸快捷档与 PNG/PDF 导出几何；色板与规格不兼容时一次可撤销变更同时切换
- 移动画布：有界网格视口（缩放/平移/双指捏合相机过渡）、手指放大镜、显式「手形/编辑工具」模式；平移缩放绝不写入图纸或跟拼进度
- PNG 导出重构：全画布不透明白底、图例独立可读字号；合并画布超限时图纸与图例分两张 PNG 打包 ZIP（client-zip）
- 跟拼进度历史、严格 ProjectFile v3（boardProfile + paletteSelection 原子保存，拒绝 v1/v2 与未知色板/规格）、发布镜像内置只读协议检查器（切流前强校验数据库）
- 工作台项目栏、色板选择器（PalettePicker）与溢出菜单（ActionOverflow）组件

### 修复

- 移动端画笔/橡皮默认精准模式（拖动对准、松手只改最终格），吸管/油漆桶按松手时最终有效格提交
- 同步严格隔离非法记录：v1/v2 或损坏项目单条隔离并结构化上报，不阻断其他合法 v3 同步；保存链路修复（此前旧格式设计保存反复 400）
- 编辑/同步/导出边界修复（协议预检契约、导出极值、双指相机等回归用例）

### 移除

- 项目文件升至 v3：v1/v2 旧项目不再读取或静默迁移；历史测试数据已在发布前显式清理（ADR-0012）

## [0.3.1] - 2026-08-30

前端体验全面重构版：工作室式布局与移动端工具坞、账号展示名，以及重构后的交互/布局/裁剪画布修复。

### 新增

- 全面重构前端体验：统一「工作室」侧边导航 + 顶栏（含溢出菜单）+ 移动端底部导航；首页/工作台/设计/色板/账户/帮助/关于全面改版；裁剪工作台采用 studio 面板与移动端工具坞
- 账号可选展示名（username，不唯一、不能登录；数据库迁移 0004）
- 新图标体系（Icon 组件）与状态外壳（StateShell）统一空态/加载/错误呈现

### 修复

- SES 备份告警模板缺失不再拒绝应用启动——告警通道降级为仅记服务器日志（验证/重置模板仍为硬要求）
- 修复裁剪画布首帧比例变形：容器首帧测量改用内容盒宽度，未测出时高度按固有比例随夹取宽度自动计算（webkit 上曾滞留超 1 秒）
- 修复局域网真机验收被 Next dev 跨域拦截（自动放行本机局域网 IPv4）与桌面页面横向溢出
- 修复重构后的交互与规范问题（E2E 03/05 三浏览器回归）

## [0.3.0] - 2026-08-28

全面审查版：只读分享、跟拼、采购清单、套装档位，以及设计系统/旅程/工程门禁修复。

### 新增

- 项目文件 v2、生成会话状态机、revision-CAS 云端同步与可恢复冲突副本
- 精确 Oklab 色彩匹配、背景原型手动取样、可取消持久 Worker 与算法 oracle/属性/性能测试
- 响应式统一页头、键盘画布、Modal 焦点循环和 WCAG 2.2 AA 自动化走查
- PostgreSQL 16 契约、原子备份恢复演练、迁移优先发布和连续稳定性门禁
- 只读分享链接：分享时固化图纸快照（不含原图与作者信息）、token 只存哈希、每设计一条（重发作废旧链）、可撤销、浏览计数不记访客标识；分享页无需登录、noindex
- 跟拼模式：逐格/整行标记「已拼」+ 进度百分比与下一未完成行；进度只存本机 IndexedDB
- 采购清单：每色号颗数 + 按包换算（每包规格可调）+ 一键复制文本
- 图纸级换色板重映射（保留手工修补）、空白画布起稿、套装档位（24/48/72/96/144 色最远点采样子集）
- 色板可视化：色板页与编辑器展示实际色块、设计卡展示用色色带
- 设计系统收敛：状态色/圆角/间距 token、统一 Notice 警示条与品牌确认弹窗、组件类回收手抄样式
- 三步指示器接线、首页真图片落区（拖拽/拍照）、生成完成播报、参数术语解释与越界提示
- PDF 中文字体构建期子集（16.4 MB → 约 3.2 MB，HarfBuzz 子集）+ UI 中文字体子集（Android 无内置圆体时的兜底）
- CI 新增 Windows 单测 job，防止 POSIX 依赖再次漂移

### 变更

- 升级到 Next.js 16、ESLint CLI 与请求级 nonce CSP；应用采用动态渲染
- 本地与云端保存状态分离；列表改为游标分页，删除使用轻量墓碑
- 大图裁剪直接收敛到工作分辨率，源图通过 Worker transfer list 传输
- 撤销栈改按总快照数限流，防 200×200 连续旋转耗尽内存
- PNG 导出超限档位动态禁用并给出可执行提示；大图裁剪降内存
- 连接池补齐语句/连接/空闲超时；同步写与备份告警端点接入限流
- 覆盖率护栏扩大至认证/会话/守卫/限流（src/lib 行 94.4% / 分支 84.0%）
- 删除 LWW 同步死代码与 HEIC/PDF 子集死开关；纸张常量与文件名清洗规则收敛

### 修复

- 修复生成参数静默失效、错误颜色映射、取消残留笔迹与重新生成覆盖手工编辑
- 修复认证令牌并发消费、会话绝对期限、邮件配额和统一 API 错误契约
- 修复 PNG/PDF 极限导出、HEIC 探测、比例锁、备份假成功与发布绕过门禁
- 修复备份告警日志注入（单行化 + HTML 转义）
- 修复读路径内联删除墓碑、未登录色板页死文案、预览画布双指缩放与 768–1023px 布局断层
- 修复对比度不达标文案、触控目标不足 44px、350px 拥挤与无障碍缺口
- 修复首页上传新图后工作台仍显示上一个设计（StrictMode 双调用吞掉交接文件，且未按「新建设计」语义跳过历史恢复；补三浏览器回归用例）
- 加宽新建/编辑色板弹窗；行内校验错误（如「颜色 #FFFFFF 重复」）改占整行显示，不再折行

## [0.2.0] - 2026-08-16

全站优化版（12 张工单：视觉/配置/安全/体验/SEO/工程健康）。

### 新增

- 全站「温柔治愈」视觉升级：奶油白底 + 主粉 + 墨紫 + 丁香紫设计系统（CSS 变量统一管理）、圆润字体、柔和粉色阴影；登录/注册/找回/重置/验证页重设计；移动端 ≤640px 展示修复；OpenGraph 分享图（og.png）
- 站点配置化：生成/导出默认参数与安全阈值改为环境变量下发（改 .env 重启即生效，无需发版），经 /api/config 注入客户端
- 生成 Web Worker 化：大图生成不冻结页面，显示进度（>300ms 才出现），支持取消；Worker 异常自动回退主线程
- HEIC 照片 WASM 兜底：Chrome/Firefox/Edge 可上传 iPhone HEIC（Safari 原生支持不变）
- 工作台接入云端自定义色板（登录后可选「我的色板」）
- 编辑器新增镜像翻转（左右/上下）与 90° 旋转（可撤销）
- PNG 导出选项面板：格子大小、图例开关、裁剪至内容开关
- 备份失败邮件告警（内部令牌鉴权端点 + backup 容器自动通知；未配置令牌时仅记日志）
- SEO：robots.txt、sitemap.xml、OpenGraph/Twitter 分享卡片、移除残留占位资源

### 安全

- 升级 Next.js 15.5.23 / React 19.2.8（修复 RSC RCE CVE-2025-66478 等）
- 攻击者视角自查修复：mutating 请求 Content-Type 守卫、速率限制、备份容器密钥面收敛、CSP 加固、登录失败统一文案等
- 修复 Firefox 下 worker 取消崩溃页面问题（改为丢弃语义）

### 工程

- 覆盖率护栏：CI 强制 src/lib 行/语句/函数 ≥90%、分支 ≥75%（实测行 92.9%），新增 40+ 单测
- 生成性能阈值收紧到 spec ≤2s；编辑器核心操作性能护栏
- 依赖评估升级 + 日志清理 + 魔法数字收拢

## [0.1.0] - 2026-08-15

首个完整版本。

### 新增

- 上传照片/像素画生成拼豆图纸（裁剪 → 调参 → 生成 → 编辑 → 导出）
- 内置 5 套国产色板：漫德 MARD / COCO / 漫漫 / 盼盼 / 咪小窝（各 291 色），支持自定义色板（500 色上限）
- 生成参数：目标宽度（20–200）、颜色数量（2–128）、抖动、主色/平均色取样、背景去除
- 像素编辑器：画笔/橡皮、吸色、填充替换、撤销重做、方向键微调
- 导出：PNG 图纸、打印版 PDF（A4 分页 + 29×29 板缝线 + 图例与色号用量清单）、项目文件（JSON）
- 账号体系：注册、邮箱验证、登录、找回/重置密码、修改密码、注销（密码校验 + 级联删除）
- 云端同步：设计随账号多设备同步（LWW 冲突解决 + 客户端时钟偏差防护）
- 安全：argon2id 密码哈希、会话令牌哈希存储、Origin 守卫、接口限流、CSP、请求体大小上限
- 测试：435+ 例单元/组件/API 测试 + 39 例三浏览器 E2E（含真实拖拽裁剪回归）

### 变更

- 授权协议采用 AGPL-3.0（基于 Zippland/perler-beads 二次开发，致谢见 NOTICE.md）
