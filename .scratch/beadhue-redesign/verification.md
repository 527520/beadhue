> 本文是原型阶段记录。正式实现与最终验证见 [implementation.md](implementation.md)。

# 原型阶段验证记录

日期：2026-09-22。环境：Codex 内置浏览器，本机静态 HTTP 服务，未连接生产 API。

## 已完成

- `node --check prototype/app.js`：通过。
- 两个主题 × 350/390/768/1440px × 11 个页面，共 88 组布局检查：页面标题存在，无水平溢出。见 `evidence/layout-checks.json`。
- 页面：发现、详情、我的设计、选图、裁剪、编辑、色板、账号、帮助、只读分享、空态。
- 搜索无结果可清除；「花与植物」返回两张示例图纸。
- 画笔点击增加一格，撤销回到零格，重做恢复；镜像及旋转可操作。
- 大屏将画布缩放至 130% 后，展开原图窗口前后主视野不变；参照视野一致。
  - 抽样坐标 `[x, y, width, height]`：`[-10.98842, 0.33136, 69.97685, 47.33728]`。
- 手机画布缩放至 169%，展开原图后上下分区，两个视野一致。
  - 抽样坐标：`[5.79335, 4.26338, 36.41329, 39.47323]`。
- 限流演示显示倒计时，取消变为「原图上传已取消」，手动重试转为上传中；未调用上传接口。
- 上传失败时公开操作展示等待原图提示；成功态公开表单需勾选授权才可提交模拟审核。
- 通过文件选择器使用仓库 `photo-wide-320x200.png` 测试非方形图片，缩小裁剪选区后可生成并显示同步参照。
- 桌面和手机截图已保存，并提供 `prototype/review.html` 双方案对比页。
- 修复验证中发现的九参数 SVG 区域绘制空白问题：先栅格化示例 SVG，再裁剪；修正镜像/旋转组合映射和隐藏窗口样式。

## 证据边界

- 此记录仅证明原型交互和上述浏览器尺寸下的布局；不是真实手机验收，也不是三浏览器产品验收。
- 原型使用示例参数和本地状态。真实算法、IndexedDB 持久化、账号、原图 COS 上传、上传频控与配额、CAS、审核、品牌兼容迁移均未实施。
- 正式源码未改变，因此未运行完整应用的类型检查、Lint、单元/数据库套件或生产构建。这些在原型获确认后的业务实施阶段执行。
- 未提交、推送、更名远端仓库、合并或部署。

## 用户评审入口

- 对比页：`prototype/review.html`。
- A/B 完整交互：`prototype/index.html?theme=gallery#discover` / `prototype/index.html?theme=candy#discover`。
- 请确认视觉方向以及导航、作品卡片、桌面悬浮窗和手机展开式对照；后续完整业务规则已保存在 `spec.md`。
