# CONTEXT

> 当前品牌与原图规则见 [ADR-0023](docs/adr/0023-beadhue-private-originals-and-candy-ui.md)。下文 D13/D24/D37/D49 中“私人原图不上云”和旧公开生命周期表述仅为历史，新规则为已验证账号的私人原图独立上云与额度计量；选图流程以 B 原型为准。
>
> 界面以 R15（D64–D72，`.scratch/ui-rebuild/`）为准：视觉与交互基准是 `.scratch/ui-rebuild/prototype/`，前端技术栈与 CSP 约束见 [ADR-0027](docs/adr/0027-frontend-stack-and-csp.md)。D30/D48/D54 的视觉与控件描述、ADR-0023 的「B 原型 / `.beadhue-ui`」界面部分仅为历史。


**豆色绘（BeadHue）** —— 拼豆图纸生成网站：上传照片 → 生成拼豆图纸 → 手动修补 → 打印/导出。开源（AGPL-3.0），部署于海外节点，完全免费、无广告。

## 术语表 Glossary

- **图纸 (pattern)**：生成的拼豆图，一个 W×H 的色块网格，每个格子对应一颗豆。
- **色板 (palette)**：某个拼豆品牌与系列的颜色集合，把品牌**色号**映射到十六进制颜色值；同一品牌的豆色绘经典版与外部资料版是不同的稳定色板。
- **可生成颜色 (engine color)**：色板中可以交给生成引擎匹配的颜色。透明材质、未知/空色号、非法 HEX 与重复 HEX 的后续别名仍可展示，但不属于可生成颜色。
- **色号 (color code)**：品牌定义的豆色编号（如漫德 C-01）。
- **抖动 (dithering)**：用离散色板的颜色分布模拟连续色阶的算法，减少色带。
- **设计 (design)**：一个保存中的作品 = 生成参数 + 色板 + 图纸 + 编辑记录；不包含原图。
- **项目文件 (project file)**：设计的序列化格式（JSON），可导出后再次导入继续编辑。
- **本地生成源 (local generation source)**：整图或选定裁剪区域缩至最长边不超过 800px 的紧密 RGBA 缓冲，仅与本机 IndexedDB 中的设计 ID 绑定，用于刷新后继续调参；不是完整原图，也不进入项目文件或云端。裁剪替换与图纸一同提交、回滚和撤销。
- **制作规格 (board profile)**：豆径与底板行列数组合；当前提供 `5mm / 29×29`、`2.6mm / 50×50` 与 `2.6mm / 52×52` 三种，50 与 52 钉距体系不得混用。
- **板缝线 (board seam)**：按当前制作规格的底板行列数划分的粗辅助线。
- **可拼格 (stitchable cell)**：跟拼中真正计入进度的图纸格；必须有颜色，且不是透明格或背景外部格（`external`）。整图进度、下一处定位和板内整行完成共用同一判定。
- **AI 功能**：本产品明确排除的类别——任何基于神经网络模型的图像生成/优化。传统算法（色彩量化、抖动）不属于 AI。
- **豆社 (community)**：豆色绘内公开展示、引用与治理图纸作品的社区空间；公开作品不是私人设计，也不是带令牌的只读分享。
- **豆社作品 (community work)**：一个稳定的公开作品身份，指向当前获准公开的修订，并承载精选、评论锁与互动事实。
- **作品修订 (community revision)**：投稿或修改时形成的不可变候选版本；审核只改变其状态，不改写已提交内容。
- **评论发布时间 (comment publication time)**：评论进入公开态的时间戳，与审核记录时间分开保存，仅作展示与审计。评论只能发表与删除，不能编辑（D55，ADR-0022）；原 15 分钟编辑窗口已退役，`edited_at` 列停写保留。
- **豆社快照 (community snapshot)**：从私人设计冻结出的可公开、可引用图纸协议，不含原图、本地生成源、私人设计正文、令牌或账号内部身份。
- **公开作者 (public author)**：豆社对外展示的作者身份，由不可枚举公开 ID、冻结显示名和作者类型组成；与内部用户身份分离。
- **引用 (reuse)**：从当前可引用的豆社修订创建一份独立私人设计的业务事实；后续两份图纸互不联动。
- **账号暂停 (account suspension)**：可恢复的账号治理状态，阻止登录和新增操作，但不改变既有豆社内容状态。
- **账号匿名化 (account anonymization)**：不可恢复地移除账号凭据和私人数据，同时以去身份形式保留公开作品、引用事实和必要治理记录。
- **分析访客 (analytics visitor)**：明确同意分析后才建立的匿名浏览器身份；数据库只保存随机令牌的哈希，不使用设备指纹。
- **分析会话 (analytics session)**：同一分析访客连续活动的 30 分钟窗口，用于有序漏斗；它不是登录会话。
- **业务事实 (business fact)**：由作品、引用、点赞、评论和举报表直接记录的产品事实；不得从可能丢失的分析事件反推。
- **审核规则版本 (moderation rule-set version)**：~~一次发布后不可变的字面词分类和风险级别集合~~ 已于 D50 退役：评论审核不再由平台维护词表，历史版本表只读保留。
- **审核判定记录 (moderation check)**：每条评论发表或修改时形成的一条审计事实，记录判定来源（腾讯云内容安全 / 同文缓存 / 本地规则 / 服务不可用）、建议、风险类别、置信度与判定依据；不保存评论正文。
- **作品原图 (work original)**：公开作品必须附带的、生成该图纸所用的原始照片，存于私有对象存储，仅原作者、审核人员和已成功引用者可取回，永不公开展示；作者撤回或注销即删除，管理员下架先封禁访问、30 天未恢复再删除。它不是本地生成源，也不进入项目文件、快照或分析事件。
- **官方批次 (official batch)**：管理员在浏览器本地批量生成官方草稿的一次工作记录；服务器不接收批次文件名或裁剪源，但每份草稿保存成功后其原图作为作品原图上传。生成完成/取消与草稿发布独立，结束状态记录该轮成功及未成功数量；有本地源的未成功项可重试，缺原图的草稿不可发布。批次的**制作规格**（底板 + 色板/套装档位）在开始前选定，随批次默认参数记录，生成的快照与之一致。
- **豆粒按钮 (bead button)**：~~纯图标操作的统一形态——圆环像一颗未上板的豆，悬停中心填色，选中态实心莓果并留一圈内环高光；点赞是一颗会「烫实」的心形豆~~。已由 D64/D65 替代：纯图标按钮统一为 `IconButton`（lucide 图标，必须有可访问名称，桌面配提示），选中态深墨；点赞为 `LikeButton`（心形填红 + 豆粒落位动效）。
- **钉板 (pegboard)**：拼豆底板的钉阵纹理，用作豆粒渲染的底、创作入口落区与空态的低对比底纹；拖入时落区高亮为主色浅底（原「钉阵变莓果色」已由 D65 替代）。
- **豆粒渲染 (bead rendering)**：R15 的签名元素——每格画成一颗带孔的圆豆放在白色钉板上，空格 / 透明 / 背景外部格只画淡钉点，无格线无板缝。用于列表与首页缩略图、我的设计缩略图、未登录详情大图（服务端 PNG，D67）以及详情 / 分享查看器、审核台与空状态插画（浏览器画布）；与之相对的是编辑时的「方格」显示。
- **内容分级 (content tiering)**：匿名访客只看服务端豆粒渲染的大图（可缩放、拖动）与颜色数、总颗数，色号、方格、色号清单、交互网格与引用需登录（D53，D67 细化）。
- **作品标签 (work tag)**：审核员或管理员挂在作品身份上的分类名（如「星星人」「海绵宝宝」），跟随作品而非某一修订；用户按标签筛选时结果全部带该标签。作者投稿时不能直接打正式标签，只能提**建议标签**（D68）。发现页作品卡不显示标签，详情页标签芯片与发现页类目 / 搜索可见（D60，D66 细化）。
- **建议标签 (suggested tag)**：作者在「公开到豆社」弹窗里给出的最多 5 个、每个不超过 8 字的标签，随修订保存（`community_revisions.suggested_tags`），不直接公开、不参与筛选；审核台展示，审核员可逐个或全部采纳为正式作品标签（写审计「采纳建议标签」）（D68）。
- **类目 (category)**：发现页吸顶类目条上的一项——「全部」「精选」加后台设为 `featured` 的启用标签，按 `sort_order` 排列，每项带 8–16 格像素图标（内置键或 `px:` 编码）；地址参数 `cat`（D66）。
- **发现 (discover)**：站点首页 `/`，豆社公开作品的浏览、搜索、类目、筛选与排序入口；主导航「发现 / 创作 / 我的」的第一项。「豆社」仍是社区空间与治理对象的领域名，界面入口统一叫「发现」（D66）。
- **作者主页 (author page)**：`/u/[publicAuthorId]`，展示公开作者的展示名、作者类型、作品数、获赞、被引用与其公开作品；只用公开作者身份，不暴露内部账号（D66）。
- **创作入口 (create entry)**：`/app` 不带 `id` 时的页面——落区选图、示例图、空白画布、导入项目文件、最近的设计；不自动恢复任何设计（`?new=1` 同义）（D66）。
- **新建图纸 (new drawing)**：选图后的弹窗——左侧取景（原图 / 1:1 / 按底板），右侧宽度、颜色数、色板、制作规格、去背景与结果预览；首版在弹窗内生成，成功后才进入编辑器并把地址换成 `/app?id=`，取消生成留在弹窗。取代 ADR-0018 / D24 中「整图自动生成首版」的入口形态，取景可选的规则不变。
- **编辑器 (editor)**：`/app?id=` 的工作区，顶栏「编辑 | 跟拼」分段切换**编辑模式**与**跟拼模式**（`?mode=edit|stitch`，跟拼可直达）。桌面为左工具栏、居中画布、右面板（颜色 / 调整 / 信息，跟拼时为进度面板）；手机（<768）为顶栏 + 全屏画布 + 底部工具栏与底部面板，原图参照为上下分屏。导出、分享、公开都在编辑器内以弹窗完成（D72）。
- **通知 (notification)**：站内给作者的事实提醒——投稿通过 / 未通过（附原因）、作品被下架 / 恢复（不带下架理由）、作品收到新评论（不存正文）；只给已登录用户显示顶栏铃铛与未读数，打开即把露出的条目标为已读；不发邮件，保留 90 天，注销即删（D70）。
- **新手条 (intro bar)**：发现页首访显示的一行可关闭说明，关闭记在本机；取代旧 `OnboardingGuide` 的首页引导，三步教程并入帮助页「三步上手」（D69）。
- **统计同意浮卡 (consent card)**：分析同意的询问形态，左下角浮卡（手机在底栏上方），不占页面流，「不同意」「同意统计」同权重；后台不显示（D69）。
- **颗 (bead count unit)**：豆子数量的唯一界面单位（「共 N 颗」「N 颗 · M 色」），原「粒」已由 D71 替代。
- **管理端路径 (admin-scoped path)**：后台专用的缩略图与原图接口，仅管理员/审核员可调用，使用独立限流键；豆社公开接口的路径与阈值不因此改变（D61）。
- **运行日志 (system log)**：一条服务端事实记录——时间、账号与角色、掩码后的网络地址、方法/路径、状态、耗时、稳定错误码、消息、堆栈与上下文，含请求编号；只增不改，仅管理员可见（D63）。
- **慢查询调用链 (slow-query chain)**：一条超过阈值的 SQL 及其触发路径（路由 → 服务 → 数据库）的有序片段，不含参数值（D63）。
- **掩码网络地址 (masked network address)**：IPv4 抹去最后一段（`a.b.c.0`）、IPv6 抹去后 80 位；用于安全与排障，不作为访客标识（D63）。

## 已定决策 Decisions

| # | 决策 | 结论 | 轮次 |
|---|------|------|------|
| D1 | 产品形态 | 前端 + 后端：账号体系 + 云端保存（用户否决了纯前端方案） | R1 |
| D2 | 设计管理 | 可保存多个设计，随时继续编辑（本地 IndexedDB + 云端同步） | R1 |
| D3 | 色板 | ~~内置 5 套国产色板（漫德 MARD、COCO、漫漫、盼盼、咪小窝，各 291 色）~~；已由 D45 扩展为 13 套 | R1/R10 |
| D4 | 生成参数 | 两级：核心（目标宽度、颜色数量、抖动开关）+ 高级面板 | R1 |
| D5 | 编辑 | 像素级手动编辑：点涂、橡皮、油漆桶、缩放查看；移动端画笔/橡皮默认精准模式（拖动对准、松手只改最终格），连续插值需会话内显式开启，吸管/油漆桶按松手时最终有效格提交 | R1/R11 |
| D6 | 导出 | PNG 图纸（含格线、色号标注）全画布使用不透明白底，图例保持独立可读字号；合并画布超限但图纸/图例可分别导出时，以两张 PNG 打包 ZIP，不以缩小文字规避上限；另有打印版 PDF（含图例+色号用量清单）和可再导入的项目文件，不单独做 CSV | R1/R11 |
| D7 | 界面语言 | 仅中文（文案集中管理，预留未来加英文） | R1 |
| D8 | 移动端 | 手机可上传/查看/导出，并以 `100dvh` 沉浸工作区完成编辑与跟拼；编辑采用显式「手形/编辑工具」模式，桌面使用有界的非沉浸视窗；平移、缩放与双指导航绝不写入图纸或跟拼进度 | R1/R9 |
| D9 | AI | 排除一切 AI 功能（参考项目的 AI 优化功能不采纳） | R0 |
| D10 | 许可证 | 接受 AGPL-3.0，本项目开源 | R2 |
| D11 | 部署 | ~~国内正式上线，个人 ICP 备案~~；已由 D31 明确取代 | R2/R4 |
| D12 | 账号 | 邮箱 + 密码自建认证；开放注册；含邮箱验证 + 找回密码 | R2/R3 |
| D13 | 原图 | ~~不上传云端，只同步图纸项目数据~~；私人设计仍不上传，公开作品的原图由 D49 规定上传 | R2/R12 |
| D14 | 后端 | Node.js + TypeScript（与前端同语言） | R2 |
| D15 | 尺寸上限 | 200×200 豆（4 万粒） | R2 |
| D16 | 板缝线 | ~~按 29×29 拼豆板标注分块辅助线~~；已由 D46 改为跟随制作规格 | R2/R10 |
| D17 | 色板范围 | ~~仅国产五套，不做海外品牌（数据结构预留扩展）~~；已由 D45 取代 | R2/R10 |
| D18 | 浏览器 | 现代浏览器最近两个大版本（Chrome/Edge/Firefox/Safari） | R2 |
| D19 | 商业化 | 完全免费、无广告、无支付系统 | R3 |
| D20 | 输入类型 | 照片 + 透明底 PNG/像素画；JPEG/PNG/WebP 常见格式 | R3 |
| D21 | 豆型 | ~~仅 5mm 融合豆（板 29×29），不做 2.6mm 迷你豆~~；已由 D46 取代 | R3/R10 |
| D22 | 自定义色板 | 用户可新建/编辑自己的色板，随账号云端同步 | R3 |
| D23 | 分享 | ~~无公开分享页，用项目文件导出/导入完成分享~~；已被 D38 取代 | R3/R8 |
| D24 | 裁剪 | 选图后先以整图自动生成首版；预览旁可选裁剪，确认后自动更新，有手工修改先确认；取消不改图纸。完整原图只在当前解码会话保留，缺失时明确要求重新选择 | R4, ADR-0018 |
| D25 | 引导 | 轻量新手引导 + 帮助/教程页；R15 起首页引导改为发现页新手条，三步教程并入帮助页「三步上手」（D69） | R4/R15 |
| D26 | 云厂商 | 腾讯云轻量服务器 + Docker 单机（约 50-100 元/月） | R4 |
| D27 | 开源仓库 | 仅 GitHub，账号 https://github.com/527520 | R4 |
| D28 | 交付 | 一次性完整交付（开发+测试完毕后整体验收） | R4 |
| D29 | 产品命名 | **豆色绘（BeadHue）**；GitHub 仓库 slug 暂定 `beadhue`（github.com/527520/beadhue） | R3 |
| D30 | 视觉风格 | 已由 D48 取代，D48 又由 D65 取代；旧版为低饱和粉紫与系统圆体方案 | R5 |
| D31 | 备案 | 服务器在海外，无需 ICP 备案；移除「备案号：上线后填写」占位（代码留将来迁回国内时加回的注释） | R5 |
| D32 | 配置化 | 导出默认参数（PDF 格大小/页边距、PNG 格大小）、生成默认参数与安全阈值（限流/会话时长等）抽为集中配置（环境变量 + 单一配置模块），改配置即生效、无需改代码发版 | R5 |
| D33 | 本轮交付 | 无用户期：全部优化一次性开发完成，交付前全量自测（单测 + 三浏览器 E2E + 生产构建 + 人工走查），整体验收 | R5 |
| D34 | 安全追加 | CSP 内联脚本收窄（nonce）、备份失败告警、限流表定期清理、备份容器固化 rclone；另做一轮攻击者视角自查并修复发现 | R5 |
| D35 | 本轮范围 | 做：HEIC 兜底、工作台接入云端自定义色板、生成 Web Worker 化+进度+取消、镜像/旋转、PNG 导出选项 UI、SEO 包（OpenGraph/robots/sitemap）、pdf-lib 按需加载、PDF 字体子集、覆盖率护栏、性能阈值收紧、配置化；不做：打印排版自定义、换色板对比、模板库、多图拼版、深色模式、站点统计、首屏性能监控 | R5 |
| D36 | 同步冲突 | 本地优先保存；云端资源使用单调 `revision` + `baseRevision` 条件写。冲突保留云端原件，并创建有明确提示的本地冲突副本，不按客户端 `updatedAt` 静默覆盖 | R6 |
| D37 | 本地生成源 | 新上传设计在 IndexedDB 独立保存整图或选区缩小后的最大 800×800 RGBA 生成源，使同一浏览器刷新后可继续调参；仅生成成功后与对应图纸原子保存，失败/取消/撤销恢复匹配的源。项目文件、同步 API 和云端数据库不保存原图或生成源 | R7, ADR-0018 |
| D38 | 分享 | 只读分享链接：分享时固化图纸快照（不含原图/生成源/作者信息），token 只存哈希、每设计一条（重发即作废旧链）、可撤销、浏览计数不记访客标识；分享页 noindex。取代 D23 | R8 |
| D39 | 跟拼模式 | 按当前制作规格的板块逐格/局部行标记「已拼」+ 进度百分比；移动端默认手形浏览，标记只在未形成导航手势的短点按 `pointerup` 提交；进度仍只存本机 IndexedDB（不进项目文件、不上云），图纸尺寸变化即失效 | R8/R9/R10 |
| D40 | 采购清单 | 每色号颗数 + 按包换算（每包规格可调）+ 一键复制文本 | R8 |
| D41 | 换色板 | 图纸级重映射：逐格换到新色板最近色，保留全部手工修补；不再要求本地生成源 | R8 |
| D42 | 起稿 | 空白画布起稿：不必先上传图片即可进入工作台；快捷尺寸按当前制作规格的底板行列数计算 | R8/R10 |
| D43 | 套装档位 | 生成可选 24/48/72/96/144 色套装子集（最远点采样，确定性），只显示不超过当前色板可生成颜色数的档位，避免生成买不到的色号 | R8/R10 |
| D44 | 色板可视化 | 色板页/编辑器/设计卡展示实际色块与用色色带（291 色不再只是数字） | R8 |
| D45 | 多系列色板目录 | 用户可见 13 套内置色板：五套冻结的豆色绘经典版（漫漫 `#F3C1C0` 含已确认的 `S7` 纠错），以及固定在 `HansBug/pindou-color-data@178dafbc9e77d3de556550dbd058270200129186` 的八套资料版；外部 ID 包含来源版本，数据随应用离线发布，不在构建或运行时追踪上游。完整展示数据与去除不可生成项后的引擎颜色由目录模块统一提供；特殊材质的屏幕 RGB 只是近似参考 | R10 |
| D46 | 制作规格 | 提供 `5mm / 29×29`、`2.6mm / 50×50`、`2.6mm / 52×52`；Artkal C/M 仅支持两种 2.6mm，MARD 221 支持三种，其余内置色板仅支持 5mm，自定义色板支持三种。50 与 52 底板不兼容；选到不兼容色板时原子切换到该色板默认规格 | R10 |
| D47 | 项目与分享协议 | `ProjectFile v3` 只接受并写出完整的 `boardProfile + paletteSelection { palette, kitTier }`；v1/v2 明确拒绝，不提供迁移或默认值。分享快照 v3 独立保存名称、创建时间、制作规格、色板定义与图纸，不包含套装档位或生成参数，也不读取旧快照。当前仅有测试数据，发布前在环境外显式清理，不在应用中保留自动清库代码；IndexedDB 与数据库表结构不因协议升级 | R10 |
| D49 | 作品原图 | 公开作品必须附带原图：用户申请公开时须明确同意「原图将上传至服务器」，不同意不能公开；管理后台官方批量直接上传。原图存私有 COS 桶、经服务端代理读写、绝不公开链接；仅原作者、管理员、已成功引用者可取回。引用成功时原图交付到引用者浏览器（工作台会话原图 + 800px 生成源），此后两份互不联动。作者撤回/注销即删对象；管理员下架先封禁访问，30 天未恢复再删除。已引用者本机副本保留。取代 D13 与官方批次「不接收原图」 | R12, ADR-0019 |
| D50 | 评论审核 | 放弃后台字面词版本维护，改为本地结构检查 + 反刷限流（每账号每小时/每天、每 IP 每小时、5 分钟突发与重复）+ 同文缓存 + 日预算 + 腾讯云 `TextModeration`：`Pass` 直发、`Review` 待审、`Block` 拒绝（评论以 `rejected` 留档，仅治理台可见，可复核后公开）。服务未配置、失败或超预算一律进待审并在系统信息页告警；每次判定写审计记录，不含正文 | R12, ADR-0020 |
| D51 | 作品标签 | 由审核员/管理员在作品管理中用标签输入框对作品打标（可现打现建、可批量），标签挂作品而非修订；作者投稿不再选标签。打标写审计但不强制手填理由（自动理由「标签调整」），是对「所有管理写入需要理由」的有意放宽。豆社按标签名筛选，搜索同时匹配标签 | R12 |
| D52 | 列表缩略图 | ~~带格线与板缝线~~（渲染风格已由 D67 改为豆粒渲染，其余不变）豆社列表、首页货架、我的投稿与后台列表统一使用服务端按修订渲染的 PNG（不可变、长期缓存），与详情页观感一致；列表 DTO 保留 48×48 派生预览仅供本地未保存项使用；官方批量卡片在批次停下后同样改用该 PNG，生成期间才用派生预览 | R12/R13 |
| D53 | 豆社反爬 | 节流 + 内容分级，不引入人机验证脚本：`proxy` 对豆社列表/详情/sitemap 按 IP 每分钟节流；公开读接口按 IP 每小时限流；点赞/举报/引用/投稿/原图上传/删评按账号 + IP 限流；匿名访客的详情页与详情 API 只给服务端渲染的 1440px 大图（无色号；R15 起为豆粒渲染、可缩放拖动，见 D67）与统计，色号网格、交互查看器与「用这张制作」需登录；sitemap 只列近期作品并分页；分页游标 HMAC 签名；所有 429 带 `Retry-After`；阈值全部配置化 | R13, ADR-0021 |
| D54 | 按钮与控件 | 已由 D64（组件与原语）/ D65（颜色与选中态）取代，下文仅为历史：保留 D48 色板与字体，新增「用色纪律」：莓果只做线与点（每个视区最多一个实心主按钮、选中态、徽标、焦点环），大面积只用白与纸白。全站按钮收敛为 `Button`（primary/secondary/quiet/danger）与圆形「豆粒」`IconButton`（删除=垃圾桶、点赞=心、举报=旗、编辑=铅笔），不再有无边框彩色小字；原生日期/数字/折叠/复选控件由 react-aria 品牌化控件替代，原生只作无 JS 降级；落区与空态用钉板点阵纹理 | R13 |
| D55 | 评论不可编辑 | 评论只能发表与删除（删除需二次确认），编辑能力连同 15 分钟窗口整体移除；`edited_at`/`published_at` 列停写保留，不做破坏性迁移；评论列表改游标分页（每页 30） | R13, ADR-0022 |
| D56 | 后台与官方批量 | 管理后台保留全部模块与 API，重做外壳（模块图标 + 待办角标、去编号）、页头、双栏模板与共享组件（理由 + 确认区、筛选条、分页、状态徽标、空态），新增 `/admin` 待办总览页，后台不弹统计同意横幅；官方批量改为四步流程（选图 → 参数与裁剪 → 生成 → 核对与发布），制作规格（底板 / 色板 / 套装档位）可选并随批次记录，服务端校验规格与色板兼容。R14 后新增「运行日志」模块（共 11 个） | R13/R14 |
| D57 | 官方草稿可再编辑 | 批次生成完成后仍可改标题、统一参数、逐项参数，并逐项「重新生成」或进入精细编辑器改图纸；未发布草稿**原地修订**同一份修订行（修订号不变、原图绑定不变、不占新的草稿名额），已发布修订继续不可变；统一参数变化只影响未生成的项，已保存草稿必须显式重新生成；制作规格（底板/色板/档位）仍在开始前冻结 | R14, ADR-0024 |
| D58 | 后台列表分页 | 管理后台所有列表默认每页 10，可选 10/20/50/100 并按模块记住；显示总数、总页数与页码，支持跳页；桌面（≥901px）双栏布局下列表自身滚动而窗口不滚动，手机保持页面滚动。豆社公开列表继续用签名游标（每页 24、只有下一页） | R14 |
| D59 | 标签管理与打标 | 「正式标签」更名「标签管理」；件数由 join 聚合真实统计（全部挂该标签的作品 + 其中公开数），作品管理不再显示件数；可从一个标签出发批量挑选**尚未打过该标签**的作品打标（带服务端缩略图、一次 ≤50 件）；新建标签不必填写操作理由（审计写固定理由「标签管理：新建标签」），改名/停用/合并仍需理由 | R14 |
| D60 | 豆社标签呈现与筛选 | 作品标签不再显示在豆社列表卡片上，详情页保留；~~标签筛选从「最多 12 个热门芯片」改为可搜索的单选控件（全部标签 + 数量，键盘可用），热门芯片收敛为前 8 个快捷入口，URL 仍是 `?tag=<名称>`~~（R15 起由 D66 的发现页类目条 `?cat=<名称>` 与搜索取代，旧 `?tag=` 并入 `cat`）；筛选与计数统一要求标签启用且未被合并 | R14/R15 |
| D61 | 后台与公开接口限流分离 | 后台官方批次改走管理端专用缩略图与原图接口（`community:moderate` / `official:manage`），使用独立配额键与阈值，豆社公开接口的路径与阈值完全不变；公开路由与管理端路由共享同一份缩略图缓存 | R14, ADR-0025 |
| D62 | 安全与资源成本加固 | Caddy 加访问日志、请求体上限与超时；容器加日志轮转上限、内存/CPU/PID 上限、只读根 + tmpfs、`cap_drop` 与 `no-new-privileges`；缩略图先查缓存再读库、每次请求都计限流；原图 GET 加账号配额 + ETag/304 + 有界字节缓存；`robots.txt`/sitemap 改 `count(*)` 并缓存、纳入节流；新增按账号读配额与「每小时不同作品数」上限（新账号更紧）；邮件日额度拆成「新账号 / 已建立账号」两档；登录失败按邮箱计数并在 15 分钟内 10 次后锁定 15 分钟；Dependabot + Trivy 扫描。备份容器改用独立 `BACKUP_COS_*` 凭据 | R14 |
| D63 | 运行日志可观测 | 自建运行日志：`system_logs`（时间、账号、角色、掩码网络地址、方法/路径、状态、耗时、错误码、消息、堆栈、上下文、请求编号）与 `slow_queries`（超过阈值的 SQL + 调用链）；`onRequestError` 覆盖 RSC 渲染错误，API 未知错误分支覆盖 500，客户端错误边界单独上报；连接池与 `pg_stat_activity` 只读展示，不启用 `pg_stat_statements`；错误日志保留 30 天、慢查询 14 天，仅管理员可见；网络地址按掩码存储（IPv4 抹末位、IPv6 抹后 80 位）并在隐私政策中披露 | R14, ADR-0026 |
| D64 | 前端技术栈 | shadcn/ui 组件模式 + Base UI（`@base-ui/react`）原语 + Tailwind v4 设计令牌 + `lucide-react` + `class-variance-authority` / `clsx` / `tailwind-merge`；日期范围用 `react-day-picker`（只用其静态 CSS）。新组件在 `src/components/ui/`，页面只组合组件与令牌工具类，不写十六进制色值与任意字号 / 圆角（单测护栏扫描）。Base UI 运行时 `<style>` 注入关闭、改由 `theme.css` 静态提供，生产 CSP 不放宽。旧 `react-aria-components`、`beadhue.css` 与 `globals.css` 旧组件类在票 13 删除，`globals.css` 只留令牌、字体与极少量基础规则。取代 D54 的控件方案 | R15, ADR-0027 |
| D65 | 视觉基准 | `.scratch/ui-rebuild/prototype/` 即验收标准：令牌取原型 `tokens.css`，组件形态取原型 `components.css` 与各页原型；主色豆蓝 `#3160E6`，选中态深墨，每视区最多一个主按钮，禁用不用半透明；正文字体 `BeadHue Text`，`BeadHue Round` 只用于标志字；七级字阶；断点 640/768/1024/1280/1536；签名元素为「豆粒渲染」，界面其余部分保持安静。取代 D48 的纸白 / 莓果配色与 D54 的「用色纪律」；后台与用户端同一视觉语言 | R15 |
| D66 | 信息架构与路由 | 主导航「发现 / 创作 / 我的」（手机底栏「发现 · ＋ · 我的」）。发现 `/`（`?q=&cat=&sort=&size=&colors=&spec=&since=`）、作品详情 `/community/[id]`、作者主页 `/u/[publicAuthorId]`、创作入口 `/app`、编辑器 `/app?id=&mode=edit\|stitch`、我的 `/me`（设计）与 `/me/public` `/me/likes` `/me/palettes` `/me/settings`、公开色板库 `/palettes`、投稿深链 `/community/submit?designId=`、后台 `/admin/<section>`、账号页沿用原路径。旧路由 308 永久重定向并保留查询参数：`/community`→`/`、`/create`→`/app`、`/designs`→`/me`、`/community/mine`→`/me/public`、`/account`→`/me/settings`。站内需要登录的操作优先弹登录弹窗，成功后继续原操作；登录默认回跳 `/me` | R15 |
| D67 | 缩略图与查看器 | 列表与首页缩略图、我的设计缩略图、未登录详情大图统一为服务端**豆粒渲染**（钉板底、无格线、无板缝），渲染版本号进图片地址与缓存键（`?v=2`），取代 D52 的「带格线与板缝」。详情查看器默认豆粒，放大到每格 ≥14px 自动出现网格，色号只在方格模式且每格 ≥18px 时显示；未登录可缩放、拖动静态大图，但不提供色号、方格、清单与交互网格（细化 D53） | R15 |
| D68 | 作者建议标签 | 「公开到豆社」弹窗里的标签是**建议标签**（最多 5 个、每个 ≤8 字），随修订保存为 `suggested_tags`，不直接成为正式标签；审核台显示建议标签，审核员可逐个或一键采纳为正式标签（写审计，受每件 10 个上限约束）。D51 其余规则不变 | R15 |
| D69 | 统计同意与新手条 | 统计同意改为左下角浮卡（手机在底栏上方），不占页面流，两个按钮同权重；发现页首访显示一行可关闭的新手条；旧首次引导删除，三步内容并入帮助页 | R15 |
| D70 | 通知 | 新增站内通知：投稿通过 / 未通过、作品被下架或恢复、作品收到新评论；与触发它的审核 / 治理 / 评论写入同一事务。站内铃铛 + 列表 + 已读（打开即标露出项、可全部已读），游客不显示铃铛；官方作品、作者自评与已注销账号不写；不发邮件，保留 90 天（`NOTIFICATION_RETENTION_DAYS`），注销即删；payload 不存评论正文与下架理由 | R15 |
| D71 | 文案单位 | 豆子数量统一用「颗」（原「粒」） | R15 |
| D72 | 公开流程 | 「公开到豆社」改为编辑器内弹窗（标题、建议标签、原图上传同意、发布权确认两个勾选框，提交审核）；投稿仍是「创建草稿 → 上传作品原图 → 提交审核」三步，D49 的原图同意不变。`/community/submit` 保留为深链入口，打开对应设计的编辑器并弹出同一弹窗 | R15 |

## 关键事实 Facts

### R15：前端全面重构（2026-09-23 起）

用户于 2026-09-23 确认 `.scratch/ui-rebuild/prototype/` 原型：「严格按照原型设计；需要加接口、功能、字段的都加；一切由代理决定」。执行 `.scratch/ui-rebuild/spec.md`，票 01–14：01 令牌与组件底座 → 02 后端接口 → 03 外壳与路由 → 04 发现 → 05 详情与作者主页 → 06 我的 → 07 创作入口 → 08 编辑器 → 09 跟拼与手机编辑器 → 10 管理后台 → 11 通知 → 12 分享 / 静态 / 错误页 → 13 清理与文档 → 14 全量验证。业务、权限、隐私与内容合同（D1–D63）不变，除 D64–D72 明确修订处；图纸协议、项目文件 v3、同步 CAS、跟拼进度存本机均不变。

**路由表（D66）**

| 页面 | 路由 | 旧路由（308） |
| --- | --- | --- |
| 发现 / 搜索 | `/`、`/?q=&cat=&sort=&size=&colors=&spec=&since=` | `/community` |
| 作品详情 | `/community/[id]` | — |
| 作者主页 | `/u/[publicAuthorId]` | — |
| 创作入口 / 编辑器 | `/app`（入口，`?new=1` 同义）、`/app?id=&mode=edit\|stitch` | `/create` |
| 我的 | `/me`（设计）、`/me/public`、`/me/likes`、`/me/palettes`、`/me/settings` | `/designs`、`/community/mine`、`/account` |
| 色板库（公开） | `/palettes` | — |
| 投稿深链 | `/community/submit?designId=` | — |
| 只读分享 | `/s/[token]`（noindex） | — |
| 后台 | `/admin`、`/admin/<section>` | — |
| 账号 | `/login` `/register` `/forgot-password` `/reset-password` `/verify-email` | — |

**页面结构**
- **站点外壳**：桌面顶栏（标志、文字导航「发现 / 创作 / 我的」、胶囊搜索与建议面板、通知铃铛、「上传图片」、头像菜单或登录）；手机顶栏 + 底栏「发现 · ＋ · 我的」（二级页不显示底栏）；页脚；统计同意浮卡；登录弹窗（手机为底部面板）。
- **发现**：吸顶类目条、筛选（尺寸 `size` s/m/l、颜色数 `colors` few/mid/many、制作规格 `spec`、时间 `since`，实时显示「显示 N 张图纸」）、排序（推荐 `rec` / 最新 / 最多喜欢 / 最多引用）、已选芯片、搜索结果标题与空结果、新手条、作品卡网格与「加载更多」（带游标的普通链接，无脚本可翻页）。
- **作品详情**：面包屑与标签芯片、作者与发布时间、喜欢 / 分享（复制链接、保存分享图）/ 更多（举报）；左查看器（豆粒 / 方格、缩放、网格、色号、板块、全屏），右吸顶制作卡（尺寸 / 颜色数 / 总颗数、规格与底板数、色号清单、「用这张制作」、下载图纸 PNG、许可说明）；讨论（最新在前）、相似作品、作者的更多作品。
- **创作入口**：落区、示例图、空白画布、导入项目文件、最近的设计 → 「新建图纸」弹窗生成首版。
- **编辑器**：编辑模式（工具、颜色 / 调整 / 信息面板、原图参照、导出 / 分享 / 公开弹窗）与跟拼模式（浏览 / 标记、进度、板块总览、当前行颜色序列、「完成本行」、上一行 / 下一行）；手机为底部工具栏 + 底部面板，跟拼为顶部进度胶囊 + 底部「上一行 ｜ 完成本行 ｜ 下一行」。
- **我的**：头部统计（设计 · 公开 · 获赞）+ 页签：设计（搜索、状态、排序、网格 / 列表、复制、导出）、公开作品（审核中 / 已公开 / 未通过附原因 / 已下架）、喜欢、色板、账号设置（资料、登录与安全含登录设备、原图空间、隐私、注销）。
- **管理后台**：浅色侧栏（工作台 / 内容 / 用户 / 系统分组与待办计数）、顶栏搜索；总览（指标卡、待办、7 日趋势、服务状态）与作品、审核、评论、举报、标签（像素图标、类目开关、排序）、批次、人员、分析、审计、日志、系统模块共用数据表格 + 右侧抽屉（手机底部面板）。
- **其他**：只读分享页与详情同一两栏栅格（色号 / 方格对所有访客开放，D38 不属于 D53 内容分级）；帮助 / 关于 / 隐私 / 社区规范 / 版权为统一文章版式；404、错误页、失效分享为豆粒插画整页空状态。

**新增接口与字段**（示例见 `.scratch/ui-rebuild/issues/02-backend-apis.md`、`06-me.md`、`10-admin.md`、`11-notifications.md` 的 Comments）：迁移 `0020_discovery_notifications`（`notifications` 表、`community_revisions.suggested_tags`、`community_tags.icon` / `featured`，`sort_order` 复用 0007 已有列）；我的设计缩略图 `GET /api/designs/:id/thumbnail`；发现列表新筛选参数与 `total`；详情 `beadCount` / `colorUsage`（未登录为 null）/ `largeImageUrl`；喜欢列表、相似作品、作者、搜索建议、我的统计、登录设备、后台趋势、采纳建议标签、通知三个接口；后台审核员读原图 `GET /api/admin/community/revisions/:id/original`。`colorUsage[].name` 是按 HEX 推导的描述性色系名（色板目录没有官方颜色名）。

**实施中确认的事实**
- 新旧两套 Tailwind 构建过渡期共存：新构建 `src/app/theme.css` 用最后一层 `ui`、不引入 preflight、与旧 `:root` 同名的令牌写在 `@theme inline`；新界面只在带 `data-ui` 的根内生效。旧组件已整体迁到 `src/components/legacy-ui/`（macOS 大小写不敏感，`Button.tsx` 与 shadcn 的 `button.tsx` 不能同目录），随票 13 删除。
- Base UI 只有 Select（`alignItemWithTrigger`）与 ScrollArea 隐藏滚动条会渲染 `<style>`，只有 Tabs.Indicator / Slider.Thumb 在 `renderBeforeHydration` 时渲染 `<script>`；根布局 `CSPProvider disableStyleElements` 关闭前者，后者不开启（ADR-0027）。
- 弹出层层级：弹窗与抽屉遮罩 z-80，Popover / Menu / Select z-85，Tooltip z-88，提示条 z-90；弹窗内可拖动区域要加 `data-base-ui-swipe-ignore`。
- `theme.css` 成功 / 警告软底色调浅（`#f0f9f4` / `#fffaf2`），使徽标文字对比度过 AA。
- 后台作品排序只作用于当前页、筛选不显示分面计数、下架通知不带理由——均为接口事实所限的有意偏差。

### R14：后台与豆社整改（2026-09）

用户在 R13 验收后提出 15 项问题，执行 `.scratch/admin-round-3/spec.md`：后台按钮尺寸体系统一（根因是不分层的 `button:not([tabindex="-1"])` 44px 下限盖掉了 `.btn-sm`/`.btn-xs`，同一套类名在 `<button>` 上是 44px 而在 `<a>`/`<label>` 上是 36/32px）、发布确认弹窗两位数序号被裁、官方草稿生成后可原地再编辑并提供精细编辑器、保存标签不再重拉列表且不需要操作理由、所有列表分页与列表内滚动、作品管理公开状态筛选、标签管理改版（更名/对齐/批量打标/件数修正/新建免理由）、豆社列表不显示标签并改标签筛选控件、手机两列卡片、后台发布脱离豆社公开限流、代理与容器加固、应用层成本与防爬补强、自建运行日志/慢查询/连接池可观测。

已确认的根因（都有代码位置与复现）：标签件数恒为 0 是 drizzle 把 `sql` 模板里的外层列渲染成不带表名的 `"id"`，被 Postgres 解析成内层 `community_work_tags.id`（`cwt.tag_id = cwt.id`）；缩略图每次请求都在查缓存**之前**读取整份快照并做 zod 解析；原图 GET 无限流无 ETag；`/robots.txt` 既不节流又整表扫描；人员管理头像因 `> span` 选择器同时命中 `.admin-avatar` 而被放进第 2 列且丢失 `place-items: center`。

### R13：全站 UI 重构与豆社反爬（2026-09-07）

用户在 R12 验收后指出按钮多为彩色小字、首页大红块、详情右栏堆叠、官方批量难用且缩略图失真、后台原始、原生日期选择器、评论不该可编辑、豆社缺反爬。执行 `.scratch/site-ui-overhaul/spec.md`：01 按钮体系 → 02 表单控件 → 03 首页 hero → 04 详情页 → 05 移除评论编辑 → 06/07 后台外壳与模块 → 08 官方批量 → 09 反爬。官方批量卡片「放太大」的根因是把 48×48 派生预览拉满 4:3 卡片且不画格线；`btn-ghost` 曾被用 8 处却从未定义；`admin-page-header` 是一行 flex 导致眉题/标题/说明横向散开。开发环境的进程内 PGlite 对元数据路由（sitemap/robots）不可见，二者在数据库不可用时退回静态条目；生产走 PostgreSQL 不受影响。

### R12：豆社第二轮整改（2026-09-06）

用户确认执行 `.scratch/community-round-2/spec.md`：修复局域网（非安全上下文）下 `crypto.randomUUID` 缺失导致选图无响应；列表缩略图改为服务端渲染 PNG；作品级标签与快速打标；公开作品原图上传私有 COS 并按权限交付引用者；官方批量改卡片式并支持全选；评论审核外部化到腾讯云文本内容安全；管理后台与使用统计全部改为使用者能看懂的中文；隐私政策、社区规范与版权声明改为正式表述。开源敏感词库基本都带政治词，与本项目「不自行维护政治词库」的边界冲突，因此选择第三方内容安全服务而非内置词表。

### D48：温暖、轻快、有手作感（2026-09-05）

> 已由 D65 取代（R15）：下文的纸白 / 莓果配色、Noto Sans CJK SC 与寒蝉全圆体、React Aria Components 控件均为历史。

用户批准全站视觉与交互重塑（`.scratch/site-visual-refinement/spec.md`）：纸白/深墨/莓果/叶绿/蜂蜜黄，Noto Sans CJK SC 正文与少量寒蝉全圆体宣传标题，字体离线随仓库交付；真实图纸/用色色带与选中豆粒落位形成辨识度，不增加无关像素装饰。统一响应式选择器（React Aria Components）、44px 触控、轻快反馈及减少动态支持。允许信息层级重排，不改变业务与隐私合同。

- 基底项目 Zippland/perler-beads：Next.js 15.3 + React 19 + TypeScript + Tailwind 4，无状态库、无 UI 库、无测试；**AGPL-3.0**，约 863 stars，2026-05 仍活跃。
- 参考项目 liangdabiao/perler-beads-ai：自称 Apache-2.0 是对 AGPL 上游的无依据再许可；唯一值得借鉴的非 AI 功能是图片裁剪（react-cropper）。
- 上游算法：按格子取 Dominant/Average 色 + Oklab 感知距离最近色匹配 + 全局频率合并；背景去除为边界洪泛填充；无抖动（需自建）。
- 上游缺失需自建：PDF 导出、项目文件格式、后端/账号、测试体系、抖动。
- 外部色板资料固定取自 `HansBug/pindou-color-data` 提交 `178dafbc9e77d3de556550dbd058270200129186`（MIT）；仓库只保存经过校验的最小快照，不在构建或运行时访问 GitHub。旧五套豆色绘色板继续使用各自冻结的数据文件，默认仍为 MARD 豆色绘经典 291 色。
- git 已可连通 GitHub（ls-remote 验证通过）；本机 DNS 被劫持，域名可用性探测不可信，以腾讯云域名注册查询为准。
- 完整调研：`.scratch/photo-to-pattern/research/upstream-facts.md`。

## 待定 Open

- **R15 进行中**：`.scratch/ui-rebuild/`，票 01–13 已合入 `feat/beadhue-ui-rebuild`（13 删除了 `legacy-ui`、`beadhue.css`、旧组件类与 `react-aria-components`，护栏扩到全部页面；`/community/submit` 已接到编辑器公开弹窗（D72）；数量单位已统一为「颗」（D71））。14 全量验收与终审走查未做。已知待办：自定义色板的「导入」入口随旧色板编辑器删除，需按新界面补回；迁移 `0020` 只在 PGlite 验证，部署前需在 PostgreSQL 16 复核。**未发版、未部署**。
- **R14 已实现，待发版**：`.scratch/admin-round-3/`（15 项问题、15 张票全部完成）。本地门禁：lint / typecheck / `npm run test`（219 文件、1679 通过） / 覆盖率（90·75 门槛） / 性能 / build / E2E 三浏览器全绿；`docker compose config` 通过。**未发版、未部署**。部署前必做：服务器 `.env` 增加 `BACKUP_COS_SECRET_ID`/`BACKUP_COS_SECRET_KEY`（备份容器不再回退 app 的 `COS_*`），GitHub Secrets 同步同名两项；Caddyfile 与容器只读根只做过人工/`compose config` 核对，需在测试部署上冒烟一次；迁移 `0019` 未对真实 PostgreSQL 执行（PGlite 每次重放验证）。生产对比基线仍是 0.5.0。
- 无产品决策待定。仓库加固计划见 `.scratch/repository-hardening-2026-08/`；完成本地门禁不等于已发布，仍须通过可用环境中的 PostgreSQL 16、Docker 及备份恢复发布门禁。
- 优化计划（`.scratch/optimization/`）：票 01–12 全部完成并全量自测通过（单测 538、覆盖率 src/lib 行 92.9%、E2E 54×3 浏览器、typecheck/lint/build 全绿、350px 移动端走查），已推送 GitHub（6 个 commit，`497169b..43e5697`）。
- 全面审查轮（`.scratch/review-2026-08/`，2026-08-26 起）：批次 A–K 全部实现。单测 918 通过 + 12 跳过（`npm run test` 全绿）、性能 7/7、覆盖率 src/lib 行 94.4%/分支 84.0%（门槛 90/75）、E2E 115 通过 × 3 浏览器（Chromium/Firefox/WebKit，8 跳过）、typecheck/lint/build 全绿。新增决策 D38–D44 已记入上表；PDF 字体子集改由 subset-font（HarfBuzz）构建（@pdf-lib/fontkit 的 CFF createSubset 产物不可用，会导致导出 OOM）。
- 0.3.0 发版：候选 `3e2942a` 经真机验收（iPhone 16 Pro Safari / 小米 15 Pro Android Chrome，含上传 capture 回归修复）与六类素材算法并排验收，证据 attestation `badd895`，tag `v0.3.0` 已推送；release workflow 构建 GHCR 镜像后按「发版升级」流程部署 doupu.fun。
- 0.3.0 已上线 doupu.fun：镜像 ghcr.io/527520/doupu:v0.3.0，迁移 0003（design_shares）已在生产应用，app/backup/caddy/postgres 全部 healthy，首页/工作台/分享 404 兜底均验证通过。已知事项：腾讯云 SES 未创建备份告警模板（生产 .env 暂用占位 ID，告警降级为仅日志）；`5680367` 已把「缺告警模板」从启动硬校验改为优雅降级，随下个版本上线。建议在 SES 控制台创建告警模板（变量 {{message}}）后更新服务器 .env。
- 0.3.1 发版：候选 `699a8f9`（前端体验全面重构 `e002dd0`/`3626fc7`、局域网验收与桌面溢出 `c073b8b`、裁剪画布首帧变形与 CI E2E 修复 `4d7ed57`、SES 告警降级 `5680367`）经真机验收（iPhone 16 Pro Safari / 小米 15 Pro Android Chrome）与六类素材算法并排验收，证据 attestation `e7f70cd`，tag `v0.3.1` 已推送。
- 0.3.1 已上线 doupu.fun：镜像 ghcr.io/527520/doupu:v0.3.1，迁移 0004（users.username）已在生产应用，app/backup/caddy/postgres 全部 healthy；生产 E2E（CSP/RSC/生成 Worker/首页落图流程）冒烟通过，/about 展示 0.3.1。SES 备份告警模板仍未创建（告警保持仅日志；降级修复 `5680367` 已随本版上线），建议在 SES 控制台创建告警模板（变量 {{message}}）后更新服务器 .env 的 `SES_ALERT_TEMPLATE_ID`。
- 0.4.0 发版：候选 `2d851fb`（色板目录 13 套与板型档位 `7828969`、工作台交互同步与图片导出修复 `249e0af`、版本与更新日志 `dc4e15a`、E2E 设备模拟断言收敛修复 `2d851fb`）经真机验收（iPhone 16 Pro Safari / 小米 15 Pro Android Chrome）与六类素材算法并排验收，证据 attestation `fc383d5`，tag `v0.4.0` 已推送。
- 0.4.0 已上线 doupu.fun：镜像 ghcr.io/527520/doupu:v0.4.0，发布前按 ADR-0012 显式清理了 3 条 v1 测试设计（先导出备份 `/opt/doupu/legacy-v1-designs-20260831.csv`，再按墓碑规范软删并递增 revision）；切流前协议 v3 只读检查两次通过（0 活动设计、0 分享），无新增迁移，app/backup/caddy/postgres 全部 healthy；生产 E2E（CSP/RSC/生成 Worker）冒烟通过，/about 展示 0.4.0。SES 备份告警模板仍未创建（告警保持仅日志）。
- 网络备注：本机 DNS 被劫持（github.com 解析到不可达 IP），HTTPS 推送失败；改用 SSH（`git push git@github.com:527520/beadhue.git main`）推送成功。SSH 22 与 ssh.github.com:443 均可达。

## 代理负责的工程决策（写入 ADR，用户可反驳）

- 数据库选型（倾向 PostgreSQL）、ORM、迁移工具
- 认证实现细节（argon2 密码哈希、会话管理、验证邮件）
- 同步冲突策略（ADR-0009：单调 revision + 条件写 + 本地冲突副本）
- 测试体系（Vitest 单元 + Playwright E2E）、CI（GitHub Actions）
- 备份方案（pg_dump + 腾讯云 COS）
- 边界处理：HEIC/EXIF 方向/动画 GIF/超大图、损坏文件、离线断网、磁盘配额
- PDF 生成库（倾向 pdf-lib）、图纸渲染细节（格子像素尺寸、色号标注密度）
